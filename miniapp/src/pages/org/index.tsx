import { View } from '@tarojs/components';
import OrgPageRoot from './OrgPage';

export default function OrgPageEntry(): JSX.Element {
  return <View className="org-page-entry"><OrgPageRoot /></View>;
}
